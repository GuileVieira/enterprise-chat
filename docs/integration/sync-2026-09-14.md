# Integração de produção — 14/09/2026

Estado atual: merge local concluído em `prod/sync-2026-09-14`, com 421 conflitos resolvidos. Não enviado nem implantado. Prontidão de produção depende do ensaio de dados/configuração descrito no relatório associado.

## Verificação final

- Build integrado: 5 tarefas concluídas com sucesso (54,51 s), conforme `/tmp/orqest-sync-build-integrated-root.log`.
- Typechecks dos cinco pacotes passaram. Coordenador confirmou novamente packages/api e client após corrigir callbacks e contrato dos ícones; roles/isolation: 144/144 e admin users: 49/49 testes no executor.
- ESLint (87,8 s), Prettier (34,8 s) e imports (0,9 s) passaram no conjunto do merge. Testes de migração de configuração passaram no coordenador (49,4 s).
- Testes focados e decisões registrados abaixo. Os checkpoints seguintes são históricos: pendências antigas não representam automaticamente o estado atual.
- Validação local concluída; merge registrado com os dois pais esperados. Nenhum push, deploy ou alteração de banco realizado nesta etapa.
- Auditoria de startup e dados: `sync-2026-09-14-data.md`. Antes de implantar, validar retenção e ensaiar em clone isolado com backup de MongoDB e storage; esse ensaio não foi executado.

- Destino: `prod/sync-2026-09-14`.
- Baseline de produção e rollback: `chore/upstream-main-integration`, `3e479c7c72f0a8a3acd1d894a11c507542d06dd5`.
- Origem sincronizada: `origin/main` / `upstream/main`, `b88d70bf63598af19e9f599a0b16d6fe9f4aef8f`.
- Worktree isolada: `/Users/guilherme/Projetos/orqest-prod-sync-2026-09-14`.
- Inventário inicial: 421 conflitos; 176 arquivos sobrepostos mesclados automaticamente. Lista completa em `sync-2026-09-14.json`.
- Nenhum deploy, push ou alteração de banco realizado.

## Decisões

1. Preservar a branch chore e suas alterações locais. Não retornar o merge para main/chore.
2. Reconciliar contratos, sem escolher ours/theirs globalmente.
3. Manter política operacional Orqest em AGENTS; incorporar cuidados upstream de cache de auth, tsdown, Jotai e tema.
4. Adotar estrutura de manifests/exports/build upstream; preservar scripts, dependências e branding locais usados. npm 11.10.0 conforme política local.
5. Unir permissões locais PROJECT/META_ADS e novas permissões upstream, com paridade Zod/Mongoose e revisão dos defaults OWNER/AD_MANAGER.
6. Manter comportamento dos projetos Orqest, ACL, reuniões e Meta Ads. Usuário confirmou manter Projetos Orqest. ChatProject upstream não substitui este produto; adaptar rotas, navegação e consumidores que colidem. Os campos persistidos são distintos (`projectId` local, `chatProjectId` upstream).
7. Traduções: merge por chave, preservar customizações divergentes, verificar chaves removidas ainda referenciadas.

## Escopo de validação original (histórico)

- Resolver todos os conflitos sem perder funcionalidades locais.
- Revisar mudanças sobrepostas sem conflito textual e novas superfícies de autorização upstream.
- Build/imports, typechecks, testes focados de contratos, testes integrados e smoke.
- Revisar compatibilidade de dados e configuração antes de qualquer proposta de deploy.

O fim dos marcadores de conflito não comprova prontidão. Os resultados posteriores estão registrados nos checkpoints abaixo.

## Checkpoint de fundação

- AGENTS, sete manifests, 14 traduções e .gitignore reconciliados e staged.
- Lockfile regenerado com npm 11.10.0; resolução sem force/legacy-peer-deps. Instalação em árvore temporária isolada passou; instalação isolada na worktree de integração concluída.
- Removido adaptador intermediário @langchain/community/Stagehand, incompatível com OpenAI upstream. DuckDuckGo usa dependência direta já existente e mantém contrato de saída. Jest focado: 4/4 segundo executor.
- Defaults de permissões revisados; links preservam comportamento anterior. Revisão dos consumidores de autorização continua pendente.
- Testes integrados, typecheck e deploy continuam pendentes.

## Verificações do coordenador

- DuckDuckGoSearch.spec.js na worktree de integração: 4 testes passaram.
- data-provider/src/roles.spec.ts: 20 testes passaram. Defaults de links preservam comportamento anterior, com gates de configuração a revisar na integração das rotas.
- Exemplos de ambiente preservam sentinelas `user_provided`; chaves fixas de exemplo seguem defaults vazios upstream.
- Decisão confirmada pelo usuário: manter o sistema de Projetos Orqest.

## Pendências concretas de revisão cruzada

- Header.tsx usa `hsl(var(--ring-primary)/0.18)`; tema upstream passa a RGB bruto. Ajustar ao integrar Header.
- Revisar chamadas raw encontradas em `/tmp/orqest-sync-raw-db-candidates.txt`: distinguir wrapper autorizado, nomes de collections e operações reais.
- Métodos `userGroup.ts`: preservar principal TENANT local e resolução `idOnTheSource` upstream; manter escaping de regex e filtro tenant simultaneamente.
- API de projetos: preservar respostas Orqest; não ligar métodos TChatProject a endpoints que retornam TProject.

## Validação parcial atualizada

- Data-provider: typecheck e build passaram; 251 testes em quatro suítes passaram.
- Tenant isolation: 65 testes passaram, incluindo preservação do filtro original com visibilidade global apenas em leitura.
- ACL/usuários/grupos: 354 testes em seis suítes passaram. Suítes de roles continuam aguardando resolução dos imports de schemas.
- Componentes compartilhados: typecheck e build `@librechat/client` passaram; artefatos CJS, ESM, tipos e CSS disponíveis. Testes focados de tema e componentes passaram; isto não valida a aplicação inteira.
- Cache de roles em revisão: chave global canônica para roles de sistema; customizadas continuam vinculadas ao tenant.
- Merge ainda aberto; nenhum commit, push ou deploy realizado.

## Checkpoint de memórias e navegação

- Núcleo de memórias: 17 testes passaram, incluindo quotas, lock, renomeação, versão, partições, IDs e isolamento tenant. Três arquivos reconciliados/staged. API de memórias ainda aguarda build dos schemas para validação.
- Roles: 77 testes de métodos/cache passaram; revisão identificou ainda normalização de nome reservado a corrigir antes de aceitar o lote.
- Navegação: 35 testes focados passaram com cobertura desabilitada e isolamento dos barrels ainda conflitantes. Seletor Orqest, movimentação por modal e pin/reorder preservados; integração completa do frontend ainda pendente.
- Hooks de dados: três suítes bloqueadas por imports conflitantes. Os dois testes Meta Ads passaram após atualizar fixtures v4 para v5; a implementação local já usava v5.
- bun.lock removido: npm/package-lock é o gerenciador e lockfile autorizado neste projeto.

- Preços/token accounting: 269 testes passaram após união das tabelas; arquivos reconciliados/staged.
- Auth UI, artifacts e ações simples revisados e com sintaxe verificada; testes completos de Auth UI ainda bloqueados por AuthContext/latex conflitantes. Não confundir resolução textual com validação de login.
- Varreduras Luna adicionais concluíram planos de utils/auth e chat/input; implementação Sol em andamento.

## Checkpoint de revisão cruzada — rodada atual

- Branch de integração continua com merge aberto; backup chore/upstream-main-integration preservado. Nenhum commit/push/deploy nesta rodada.
- Revisão compara mudanças locais desde merge-base e resultado contra upstream; ausência de marcadores não prova preservação funcional.
- Corrigidas perdas detectadas pelo coordenador: TenantFunctionsSection e payload de ferramentas locais; double submit de prompts e retry após erro; uploads de projeto no menu local e SharePoint; codeSessionToolNames para imagens geradas e encaminhamento do contexto de execução no priming.
- MCP/painel: 61 testes; detalhes de agentes: 23; formulário/payload de agentes: 19; uploads: 56; registro de configurações: 10; seletor de endpoints: 43; seleção de agentes: 10. São verificações focadas locais, não validação completa da aplicação.
- Pendências ativas: filtro duplicado de useMentions, texto colado local-only e placeholder em useTextarea, foco do editor de prompts/mobile sidebar, retirada de ProjectLandingChip/TChatProject do ChatForm, avatar padrão local em SpecIcon.
- ACL: revisão encontrou erro de índices ao separar grants TENANT e usuários; agente corrigiu mapeamento original/filtrado e adicionou regressão. Validação do coordenador em andamento.
- Restam conflitos de backend, UI e revisão semântica dos automerges; build integrado e testes finais ainda pendentes.

## Checkpoint seguinte — revisão do coordenador

- Contagem observada: 65 arquivos ainda não resolvidos no índice; 31 com marcadores e 34 sem marcadores aguardando revisão. Contagem é um snapshot, não conclusão da integração.
- Agents/Prompts, compartilhamento/Header e Mermaid/Web revisados e registrados como resolvidos. Header usa RGB e protege a conversa ativa de respostas atrasadas. Tenant ShareView consulta configuração autenticada e usa o fork de tenant.
- Validações locais do coordenador: permissões do seletor 4 testes; Agents/Search/MCP/RunCode 35; Share/Header/Export 8; config/balance/plugins 92. Seletor agora mapeia Skills e ambientes de código, com rótulos de Projects preservados.
- Pendências antigas de useMentions/useTextarea, SpecIcon e grants TENANT foram corrigidas em rodadas posteriores; ChatForm ainda aguarda aceitação final.
- Lote Files não aceito: revisão identificou respostas de exclusão que ignoravam falhas parciais, autorização insuficiente para lote misto de arquivos de projeto e particulares, retorno precoce no upload de assistants e caminho OCR incompatível com SSE. Correções e regressões solicitadas ao agente.
- Exclusão administrativa ainda não aceita: conferir configuração do usuário alvo, limpeza de links compartilhados/agendamentos e paridade com o ciclo de exclusão do próprio usuário.
- Typecheck do frontend ainda tem pendências em Projects/Meta Ads, Skills e outros consumidores. Nenhum commit, push ou deploy; merge permanece aberto.

## Checkpoint de inicializacao e interface

- Todos os conflitos textuais da interface foram revisados e registrados no indice. TypeScript completo do cliente passou antes das ultimas restauracoes visuais; repetir na validacao integrada.
- Preservados duplicacao com um clique e visual local da tela inicial; Fork/Landing: 9 testes do coordenador passaram. EmptyState: 3 testes passaram.
- Startup principal e experimental unidos, mantendo runAsSystem para operacoes globais e inicializacao upstream de ferramentas/eventos. Testes do coordenador: principal 26, experimental 24, descoberta de ferramentas 3.
- Revisao backend ainda aberta: controllers/agentes, rotas, exclusao administrativa e testes de regressao de Files. Ausencia de marcadores nao significa integracao concluida. Nenhum commit/push/deploy.

## Checkpoint de validacao integrada parcial

- Snapshot atual: 17 arquivos ainda nao resolvidos no indice, todos backend. Frontend sem alteracoes pendentes fora do indice; merge permanece aberto.
- Typechecks completos de client, data-provider, data-schemas e packages/api passaram. Esses checks antecedem as ultimas correcoes backend; nao substituem build integrado.
- Assistants V1/V2: contexto autorizado agora chega ao corpo real enviado ao modelo e a contagem de tokens; anexos repetidos deduplicados. 23 testes do coordenador passaram.
- Inicializacao de agentes: contexto de projeto encaminhado ao agente principal, handoff e inicializacao/execucao lazy. Removido codigo residual duplicado do merge; 75 testes do coordenador passaram.
- Compartilhamento publico/tenant e fork: 168 testes do coordenador passaram. Artefatos respeitam ACL/revogacao e filtros atuais; fork tenant nao copia IDs de arquivos privados do autor.
- Frontend: 44 testes de Projects/agendamento/selecao e 167 testes de fixtures/fluxos passaram. Removido ChatProject upstream concorrente; /skills/new passa pela rota parametrizada para abrir criacao corretamente.
- Pendencias concretas: 3 falhas em UserController.spec.js na rodada ampliada; contexto global/alvo no lifecycle administrativo; vinculos de projeto em todos os caminhos de upload; politica de texto derivado de OCR; controllers remotos e persistencia projectId do AgentClient. Workers receberam os casos exatos.
- Nenhum commit, push ou deploy. Validacao completa e conclusao do merge ainda pendentes.

## Checkpoint sem conflitos no indice

- Todos os 421 conflitos iniciais foram reconciliados e registrados como resolvidos. O merge ainda nao foi commitado.
- Rodadas finais do coordenador: AgentClient 258; gestao de agentes 157; controllers remotos 115; Files 202; UserController 40; admin users 49; Projects/reunioes/Meta Ads 273 testes passaram. Contagens por rodada, nao total unico da integracao.
- Memorias OWNER permanecem somente leitura para colegas: texto compartilhado sem chaves de outro usuario, inclusive com ferramentas inline; filtros atuais inspecionam esse contexto. Falha na descoberta de OWNER nao elimina memoria pessoal.
- Checks estaticos de configuracao ESLint, manifests e ciclos passaram. Todos os cinco projetos TypeScript passaram nas rodadas registradas; ainda repetir depois do build integrado.
- Build integrado iniciado com npm 11.10.0. Resultado ainda pendente neste checkpoint.
- Auditoria de deploy somente leitura: manifests/Compose e configuracao local coerentes; deploy-compose exige librechat.yaml externo, ausente nesta worktree. Nenhum deploy ou banco alterado.

## Limites da verificação global

- `npm run lint:fix` executado: 254 diagnósticos em 68 arquivos. Apenas um arquivo fazia parte do índice inicial do merge (spec de sidebar); corrigido. Os demais são código legado fora do diff e não foram ampliados neste merge. Alterações automáticas fora do escopo foram revertidas ao índice. O gate de arquivos alterados é executado separadamente.
- `npm run format` executado com sucesso. Ordenação de imports aplicada ao conjunto de arquivos do merge.
- `git diff --cached --check` encontra duas ocorrências de whitespace no inventário BSON upstream. Arquivo idêntico ao MERGE_HEAD (SHA-1 4756c876b36d7aee32858fa790f1893b8da0d14d).

- Graphify atualizado com sucesso; artefatos derivados mantidos fora do commit. O extrator reportou limitações de parsing em sete arquivos; typechecks reais passaram.

## Fechamento

- O checkout não tinha `core.hooksPath` configurado; o primeiro commit não executou hooks automaticamente. Executados explicitamente `lint-staged --diff HEAD^1..HEAD --config .husky/lint-staged.config.js` e `static-checks --against HEAD^1 --skip eslint,prettier,imports`: ambos passaram, sem novas alterações de código.
- Build, typechecks, testes focados, checks de configuração e gates do diff passaram. Não equivale a suíte completa, smoke com dados de produção nem deploy. Os gates opcionais de chaves i18n não usadas e dependências não usadas não foram executados.
- `chore/upstream-main-integration` preservada no commit inicial. Nenhuma alteração remota ou execução contra o banco de produção.
